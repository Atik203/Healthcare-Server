name: Auto Merge Pull Request

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    if: contains(github.event.pull_request.labels.*.name, 'auto-generated') && github.event.pull_request.head.ref == 'dev'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Auto-merge PR
        run: |
          echo "Auto-merging PR #${{ github.event.pull_request.number }}"

          # Add a comment before merging
          gh pr comment ${{ github.event.pull_request.number }} \
            --body "🤖 Auto-merging this pull request from dev to main..."

          # Merge the PR
          gh pr merge ${{ github.event.pull_request.number }} \
            --squash \
            --delete-branch
            
          echo "✅ Successfully merged PR #${{ github.event.pull_request.number }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Handle merge failure
        if: failure()
        run: |
          gh pr comment ${{ github.event.pull_request.number }} \
            --body "⚠️ Auto-merge failed. Please review conflicts and merge manually."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
